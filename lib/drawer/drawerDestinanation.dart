import 'package:flutter/material.dart';

class DrawerDestination extends StatelessWidget {
  const DrawerDestination({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Loading...')),
    );
  }
}